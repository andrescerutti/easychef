
<div class="navbar navbar-expand-sm navbar-dark navbar-mobile py-3 bg-white justify-content-between">
 <!-- NAVBAR MOBILE -->
 <div class="d-md-none">
  <div class="dropdown d-none-sm">
    <i class="fas fa-bars fa-2x text-primary" id="dropdownMenuButton" data-toggle="dropdown"></i>
    <div class="dropdown-menu" aria-labelledby="dropdownMenuButton">
      <% if user_signed_in? %>
       <h2>hola</h2>

      <% else %>
        <div class="dropdown-menu dropdown-menu-right" aria-labelledby="navbarDropdown">
          <%= link_to "Sign In", new_user_session_path, class: "btn btn-outline-dark mr-3" %>
          <%= link_to "Sign Up", new_user_registration_path, class: "btn btn-dark" %>
        </div>
      <% end %>
    </div>
  </div>

 </div>
  <%= link_to root_path, class: "navbar-brand" do %>
    <%= image_tag('logo.png') %>
  <% end %>


  <div class="d-none d-md-block">
    <%= simple_form_for :search, url: root_path, method: :get do |f| %>
      <div class="search-form-control form-group m-0">
        <input class="form-control string required" type="text" name="search[query]" id="search_query" />
        <button name="button" type="submit" class="btn btn-flat">
          <i class="fas fa-search"></i> Search
        </button>
      </div>
    <% end %>
  </div>
<!-- NAVBAR DESTOCK -->
  <div class="collapse navbar-collapse flex-sm-grow-0" id="navbarSupportedContent">
    <% if user_signed_in? %>
        <div class="dropdown">
          <i class="fas fa-user-circle fa-2x text-primary" id="dropdownMenuButton" data-toggle="dropdown"></i>
          <div class="dropdown-menu" aria-labelledby="dropdownMenuButton">
            <% if user_signed_in? %>
             <h2>hola</h2>

            <% else %>
              <div class="dropdown-menu dropdown-menu" aria-labelledby="navbarDropdown">
                <%= link_to "Sign In", new_user_session_path, class: "btn btn-outline-dark mr-3" %>
                <%= link_to "Sign Up", new_user_registration_path, class: "btn btn-dark" %>
              </div>
            <% end %>
          </div>
        </div>
    <% else %>
      <%= link_to "Sign In", new_user_session_path, class: "btn btn-outline-dark mr-3" %>
      <%= link_to "Sign Up", new_user_registration_path, class: "btn btn-dark" %>
    <% end %>
  </div>
    <i class="fas fa-user-circle fa-2x d-md-none text-primary"></i>
</div>


